# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Uallet' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Uallet
  
  # PONGO TODAS LAS LIBRERIAS QUE QUIERA USAR POR DEBAJO DE ESTOS COMENTARIOS
  # ACA COPIO Y PEGO LA INSTRUCCION POD QUE ME DE LA LIBRERIA
  pod 'Alamofire', '~> 5.5' #tengo que agregar la version que quiero de la libreria
  pod 'PromiseKit', '~> 6.8'
  
end
